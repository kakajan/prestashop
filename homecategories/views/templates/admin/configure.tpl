<div class="panel">
    <h3>{$module->displayName}</h3>
    <form action="{$link->getAdminLink('AdminHomeCategories')}" method="post">
        <!-- Add configuration options here -->
        <button type="submit" class="btn btn-primary">Save</button>
    </form>
</div>
